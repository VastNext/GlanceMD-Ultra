const assert=require('node:assert/strict');const test=require('node:test');const vm=require('node:vm');const fs=require('node:fs');
function load(){const els={};const doc={body:{appendChild(e){els[e.id]=e;}},getElementById:id=>els[id]||null,createElement:()=>({innerHTML:'',hidden:false,querySelector(){return {value:'',focus(){}};},querySelectorAll(){return [];}}),addEventListener(){}};const c={window:{},document:doc,localStorage:{getItem(){return '[]';},setItem(){}},console};c.window=c;c.Commands={ids:()=>['file.open','workspace.open'],run(){},has:()=>true};vm.runInNewContext(fs.readFileSync('src/frontend/command-palette.js','utf8'),c);return c;}
test('palette namespace and scoring',()=>{const c=load();assert.equal(typeof c.CommandPalette.toggle,'function');assert.ok(c.CommandPalette.score('open','workspace.open')>0);assert.equal(c.CommandPalette.score('zzz','file.open'),-1);});
test('palette open creates hidden panel',()=>{const c=load();c.CommandPalette.open();assert.ok(c.document.getElementById('command-palette'));assert.equal(c.document.getElementById('command-palette').hidden,false);});
