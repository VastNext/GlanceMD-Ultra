const assert=require('node:assert/strict');const test=require('node:test');const vm=require('node:vm');const fs=require('node:fs');
function load(){const els={};const doc={body:{appendChild(e){els[e.id]=e;}},getElementById:id=>els[id]||null,createElement:()=>({hidden:false,innerHTML:'',querySelector(){return {value:'',trim(){return '';},focus(){},addEventListener(){},classList:{contains(){return false;}}};},querySelectorAll(){return [];},addEventListener(){}}),addEventListener(){}};const c={window:{},document:doc,setTimeout,clearTimeout,console};c.window=c;c.Workspace={on(){}};vm.runInNewContext(fs.readFileSync('src/frontend/search-panel.js','utf8'),c);return {c,els};}
test('search panel namespace and toggle',()=>{const h=load();assert.equal(typeof h.c.SearchPanel.toggle,'function');h.c.SearchPanel.open();assert.equal(h.els['search-panel'].hidden,false);h.c.SearchPanel.close();assert.equal(h.els['search-panel'].hidden,true);});
test('search panel start emits structured options',()=>{const h=load(),msgs=[];h.c.window.ipc={postMessage:m=>msgs.push(JSON.parse(m))};h.c.SearchPanel.open();h.els['search-panel'].querySelector=()=>({value:'needle',classList:{contains(){return false;}},addEventListener(){},focus(){}});h.c.SearchPanel.start();assert.equal(msgs[0].command,'workspace.search.start');assert.equal(msgs[0].options.query,'needle');});
test('search panel state exposes hits',()=>{const h=load();assert.equal(h.c.SearchPanel.getState().hits.length,0);});
